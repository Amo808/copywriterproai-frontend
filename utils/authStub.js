// Temporary auth stub for demo
export const authStub = (strategy) => {
  console.log(`Auth with ${strategy} clicked - API not configured`);
  alert(`Authentication with ${strategy} is not configured yet. Please set up the backend API.`);
};
